#!/bin/sh

cfg=$HOME/.config/river
src=$cfg/src

. $src/vars
. $src/binds
. $src/tiling
. $src/rules
. $src/general

if [ ! -e "$LOCKFILE" ]; then
    . $src/autostart &
    touch "$LOCKFILE"
else
    echo "Autostart skipped, lockfile exists."
fi
