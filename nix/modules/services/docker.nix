{ ... }:

{
    # virtualisation.docker = {
    #     enable = true;
    #     rootless = {
    #         enable = true;
    #         setSocketVariable = true;
    #     };
    # };
    #
    # users.users.thatmagicalcat.extraGroups = [ "docker" ];
    # virtualisation.docker.storageDriver = "btrfs";
}
